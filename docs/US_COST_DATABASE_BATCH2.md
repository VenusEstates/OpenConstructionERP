# Extending a cost database

This document previously held a work plan for one batch of a specific engagement. Everything in
it that was about the platform rather than about that engagement now lives in the two documents
below, next to the material it belongs with.

**Adding rows to a region that already exists**, and why re-importing a corrected file reports
every row as skipped and changes nothing, is in [importing your own cost
database](./cost-database-import.md), under "Adding to a database that already exists".

**Refreshing prices on a region you already built**, what a repricing pass does and does not
overwrite, and how to read its coverage report, is in [building a regional cost
database](./US_COST_DATABASE_METHODOLOGY.md), under "Keeping it current".

For a runnable end-to-end example, see [the worked
example](./US_COST_DATABASE_PILOT.md).
